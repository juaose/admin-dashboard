ENV=dev

# DAL API Configuration
# HTTP REST API endpoint for lotto-mongo-dal-api service (server-side only - not accessible from browser)
# For local development: http://localhost:3100
DAL_API_URL=http://localhost:3100

# Public DAL API Gateway endpoint (accessible from browser)
# This MUST be set to your API Gateway URL for client-side requests
# Example: https://zanttjd6zj.execute-api.us-east-1.amazonaws.com
NEXT_PUBLIC_DAL_API_URL=

# Cognito User Pool Configuration
# These environment variables are required for authentication
# Set these in your Amplify deployment environment settings to point to your pre-existing Cognito user pool

# User Pool ID (e.g., us-east-1_3QOXrkS9v)
NEXT_PUBLIC_USER_POOL_ID=

# User Pool Client ID
NEXT_PUBLIC_USER_POOL_CLIENT_ID=

# Identity Pool ID
NEXT_PUBLIC_IDENTITY_POOL_ID=
