# Environment Configuration
# Copy this file to .env.local if you need local environment variables

# Server URL Configuration
# The app automatically detects environment, but you can override:

# Option 1: Override server URL (useful for local frontend → production server)
# NEXT_PUBLIC_SERVER_URL_OVERRIDE=https://bn-sock.srcruz.bet
# NEXT_PUBLIC_SERVER_URL_OVERRIDE=http://localhost:4005

# Option 2: Force production mode even on localhost
# NEXT_PUBLIC_FORCE_PRODUCTION=true

# Default behavior (no env vars needed):
# - localhost/127.0.0.1: Uses http://localhost:4005 (your PC)
# - Any other hostname: Uses https://bn-sock.srcruz.bet (AWS/Amplify)

# Usage Examples:
# 1. Normal development: No env vars needed
# 2. Local frontend → Production server: Set NEXT_PUBLIC_SERVER_URL_OVERRIDE=https://bn-sock.srcruz.bet
# 3. Force production URLs: Set NEXT_PUBLIC_FORCE_PRODUCTION=true

# Add other local development variables here if needed
# Remember: Variables that need to be accessible in the browser must start with NEXT_PUBLIC_
